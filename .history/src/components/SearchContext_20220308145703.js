import React from "react";

const SearchContext = () => {
  return (
    <div className="mt-3">
      <input type="text" />
      <button>Search</button>
    </div>
  );
};

export default SearchContext;
